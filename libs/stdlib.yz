func str(val) public {
	return val;
}

func must_int(num) public {
	if 0 < num {}
	return num;
}

func rand_num(min, max) public {
	_yz_invoke(_yz_cmd_rand_num, rand_num, min min, max max);
	return rand_num;
}