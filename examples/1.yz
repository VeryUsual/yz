let x = -5;
let f = "ffafafkihujyfdersrfedui";

func main(param, other_param, #arbitrary_params_allowed) {
    println(param);
    println(other_param);
    println(fake_param);
}

func other() {
    println("other");
}

if x == f {
    println("x equals f (impossible)");
}

if x == x {
    println("x equals x!");
}

println(x);
let y = x + 6 * 4;
println(y);
let z = y * 44 + 3;
println(z);

other();
main(param 4, other_param 3, fake_param 99);